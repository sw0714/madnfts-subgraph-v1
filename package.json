{
  "name": "madinarttest",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ madinarttest",
    "create-local": "graph create --node http://localhost:8020/ madinarttest",
    "remove-local": "graph remove --node http://localhost:8020/ madinarttest",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 madinarttest"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
